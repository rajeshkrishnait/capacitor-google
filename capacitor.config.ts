import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rajesh.googleimageclone',
  appName: 'google-image-clone',
  webDir: 'dist'
};

export default config;
